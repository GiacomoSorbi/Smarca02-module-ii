import React from "react";

//Originally I wanted this button to work as an anchor link leading to the events page.
//However, after reseaching ways to do it in React I decided not to implement it for now.
const ButtonToEvents = () => (
  <div>
    I am a button leading to the events page
    <ButtonToEvents />
  </div>
);

export default ButtonToEvents;
